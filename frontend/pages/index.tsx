import React from 'react';

export default function Home() {
  return (
    <main style={{ padding: 24, fontFamily: 'Inter, Arial' }}>
      <h1>ClarySwap — Stacks AMM MVP</h1>
      <p>Frontend skeleton. Connect a wallet with @stacks/connect and call contract endpoints.</p>
    </main>
  );
}
import React from 'react';

export default function Home() {
  return (
    <main style={{ padding: 24, fontFamily: 'Inter, Arial' }}>
      <h1>ClarySwap — Stacks AMM MVP</h1>
      <p>Frontend skeleton. Connect a wallet with @stacks/connect and call contract endpoints.</p>
    </main>
  );
}
